import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#222831',
        },
        secondary: {
            main: '#393E46',
        },
        info: {
            main: '#00ADB5',
        },
        background: {
            default: '#EEEEEE',
        },
    },
});

export default theme;
